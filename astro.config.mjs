import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://sergiocampbell.github.io/sacv',
  base: 'https://github.com/SergioCampbell/sacv',
});
